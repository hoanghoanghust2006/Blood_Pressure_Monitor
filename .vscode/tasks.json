{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Rebuild",
            "group": "build",
            "type": "shell",
            "command": "make clean && make -j12"
        },
        {
            "label": "Build",
            "group": "build",
            "type": "shell",
            "command": "make -j12 test=-DTEST_GLCD"
        }
    ]
}